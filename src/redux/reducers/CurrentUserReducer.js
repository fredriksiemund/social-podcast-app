const INITIAL_STATE = {
  id: 0,
  name: 'Pelle Persson',
  userImageUri: 'http://ap.church/wp-content/uploads/2018/08/Brian-Jones-Profile.jpg'
}

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state
  }
}
